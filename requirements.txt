# Elite Score Hub - Core Bot
# Simple, self-contained Telegram bot with real predictions

# Core (aiogram 3.7+ works with Python 3.8-3.12)
aiogram>=3.7.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# HTTP
httpx>=0.24.0
aiohttp>=3.9.0

# Database (SQLite - no server needed)
aiosqlite>=0.19.0

# Scheduling
apscheduler>=3.10.0

# Date utils
python-dateutil>=2.8.0
pytz>=2023.3
